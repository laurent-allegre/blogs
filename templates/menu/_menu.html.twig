
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Blog</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link {% if app.request.get('_route') == 'liste_articles' %} active{% endif %}" aria-current="page" href="{{ path('liste_articles')}}">Accueil</a>
                {% if is_granted('ROLE_ADMIN')%}
                <a class="nav-link {% if app.request.get('_route') == 'ajout-article' %} active{% endif %}" aria-current="page" href="{{ path('ajout-article')}}">Ajouter article</a>
                <a class="nav-link {% if app.request.get('_route') == 'brouillon-article' %} active{% endif %}" aria-current="page" href="{{ path('brouillon-article')}}">Les brouillons</a>
                {% endif %}
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <a class="nav-link" aria-current="page" href="{{ path('app_logout')}}">Déconnexion</a>
                {% else %}
                    <a class="nav-link {% if app.request.get('_route') == 'app_login' %} active{% endif %}" aria-current="page" href="{{ path('app_login')}}">Connexion</a>
                {% endif %}

            </div>
        </div>
    </div>
</nav>
